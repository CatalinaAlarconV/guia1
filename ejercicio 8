#include <stdio.h>
#include <math.h>

/*Este programa permite evaluar un polinomio dando distintos valores para (x) */

int main ()
{
	int x1, x2, x3, polinomio 
	printf ("ingrese valor 1: ");
	scanf ("%d",&x1);
	printf ("ingrese valor 2: ");
	scanf ("%d",&x2);
	printf ("ingrese valor 3: ");
	scanf ("%d",&x3);

	polinomio = p(x1)= pow (x1, 3) + 2 * pow (x1, 2) + 3 * x1 + 5 \n
	polinomio = p(x2)= pow (x2, 3) + 2 * pow (x2, 2) + 3 * x2 + 5 \n
	polinomio = p(x3)= pow (x3, 3) + 2 * pow (x3, 2) + 3 * x3 + 5

	//printf ("resultado valor 1: %d", x1);
	//printf ("resultado valor 2: %d", x2);
	//printf ("resultado valor 3: %d", x3);
	
	return 0 ;
}

