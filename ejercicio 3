#include <stdio.h>
#include <math.h>

int main ()
{
 	int a ; 
	int b ;
	int calculo_hipotenusa;
	printf ("indique valor de a: ");
	scanf ("%d", &a);
	printf ("indique valor de b: ");
	scanf ("%d", &b);
	
	calculo_hipotenusa = pow (c,2) = pow (a,2) + pow (b,2) ;
	
	//printf ("resultado hipotenusa : %d" , calculo_hipotenusa );

	return 0 ;
}

