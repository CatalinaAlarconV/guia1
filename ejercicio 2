#include <stdio.h>
#include <math.h>
 
/*El programa al recibir un numero entero positivo (x), calcula el cuadrado y tambien el cubo de este */

int main ()
{
	int x ;
	int cuadrado, cubo ;
	
	print ("ingrese un numero entero positivo : ");
	scanf ("%d", &X);
	
	cuadrado = pow (x , 2);
	cubo = pow (x, 2);

	printf ("resultado del cuadrado de un numero entero positivo");
	scanf ("%d", cuadrado);
	printf ("resultado del cubo de un numero entero positivo");
	scanf ("%d", cubo);

	//printf ("resultado al cuadrado: %d" , cuadrado);
	//printf ("resultado al cubo: %d", cubo); 

	return 0 ; 
}
