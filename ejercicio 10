#include <stdio.h>
/* este programa determinará el costo que tendrá un boleto de bus por los kilometros que el recorra*/

int main ()
{
	int costo_boleto, kilometro_recorrido, viajes ;
	printf ("ingrese kilometros recorridos: ");
	scanf ("%d",&kilometros_recorridos);
	printf ("ingrese numero de viajes");
	scanf ("%d",&viajes);
	
	costo_boleto: kilometro_recorrido * viajes ;

	//printf ("costo del boleto: %d", costo_boleto);

	return 0 ;
}


